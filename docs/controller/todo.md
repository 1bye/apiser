// - Think about "option", that allows to devide `handler` into segments, or conditionally run segments of `handler`. It's useful in case if you need to re-use same logic across app, but preserve code on same place.

- Think about checkpoints
